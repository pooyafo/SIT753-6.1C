pipeline {
    agent any
    environment {
        EmailAddress = 'pooyafo@gmail.com'
    }
    triggers {
        pollSCM('H/1 * * * *')  // Triggers every minute to check for new commits
    }

    stages {
        stage('Build') {
            steps {
                script {
                    // Build the project using Maven
                    echo "mvn clean package"
                }
            }
        }
        
        stage('Unit and Integration Tests') {
            steps {
                script {
                    // Run unit tests
                    echo "mvn test"
                    // Run integration tests
                    echo "mvn integration-test"
                }
            }
            post {
                always {
                    script {
                        writeFile file: 'console_log.txt', text: currentBuild.rawBuild.getLog(1000).join('\n')
                    }
                    emailext (
                        attachmentsPattern: 'console_log.txt',
                        subject: "Unit and Integration Tests - ${currentBuild.currentResult}",
                        body: 'Find the log file of the "Unit and Integration Tests" stage in the attachments.',
                        to: "${env.EmailAddress}"
                    )
                }
            }
        }
        
        stage('Code Analysis') {
            steps {
                script {
                    // Perform static code analysis using SonarQube
                    echo "sonar-scanner"
                }
            }
        }

        stage('Security Scan') {
            steps {
                script {
                    // Perform security scan using OWASP Dependency-Check
                    echo 'dependency-check --project "MyApp" --scan "./"'
                }
            }
            post {
                always {
                    script {
                        writeFile file: 'console_log.txt', text: currentBuild.rawBuild.getLog(1000).join('\n')
                    }
                    emailext (
                        attachmentsPattern: 'console_log.txt',
                        subject: "Security Scan - ${currentBuild.currentResult}",
                        body: 'Find the log file of the "Security Scan" stage in the attachments.',
                        to: "${env.EmailAddress}"
                    )
                }
            }
        }

        stage('Deploy to Staging') {
            steps {
                script {
                    // Deploy to the staging server
                    echo "aws deploy create-deployment --application-name MyApp --deployment-group-name Staging"
                }
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                script {
                    // Run tests in the staging environment
                    echo "postman-collection-runner --environment staging --collection my-tests.json"
                }
            }
        }

        stage('Deploy to Production') {
            steps {
                script {
                    // Deploy to the production server
                    echo "aws deploy create-deployment --application-name MyApp --deployment-group-name Production"
                }
            }
        }
    }
}
